# =============================================================================
# SDSS Phase Pipeline — requirements
# =============================================================================
# Design: pin only where a specific version matters for reproducibility.
# Let pip pick compatible wheels for everything else — this avoids the
# "no wheel for your Python version, compile from source for 45 min, fail" trap.
# =============================================================================

# Core scientific stack — let pip resolve, all have wheels on Colab
numpy>=1.24,<2.0
scipy>=1.11
pandas>=2.0

# EBSD (pinned — API compatibility is important here)
orix>=0.13,<0.14
diffpy.structure>=3.2

# Image processing — let pip resolve
scikit-image>=0.22
opencv-python-headless>=4.9
Pillow>=10.0

# Deep learning — do NOT reinstall torch on Colab, it's already there and CUDA-matched.
# We only add the training-time libraries that build on top of it.
segmentation-models-pytorch>=0.3.4
albumentations>=1.4
timm>=1.0

# Classical ML
scikit-learn>=1.3
xgboost>=2.0

# Uncertainty (regression conformal, Phase 4)
mapie>=0.8

# Config
PyYAML>=6.0
hydra-core>=1.3
omegaconf>=2.3

# Logging / viz
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
tensorboard>=2.15

# Testing
pytest>=8.0