# =============================================================================
# SDSS Phase Pipeline — requirements
# Pinned for Colab (Python 3.11) reproducibility.
# =============================================================================
#
# Notes on pinning:
# - Versions are pinned to a known-working set as of the project start date.
# - When Colab updates, `pip install -r requirements.txt` will pin back to these.
# - Do NOT `pip install --upgrade` in Colab notebooks — you'll break repro.
# =============================================================================

# --- Core scientific stack ---
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2

# --- EBSD / crystallography (Python-native, no MATLAB dependency) ---
orix==0.13.3              # .ang/.ctf/.h5ebsd parsing, phase/orientation handling
                          # https://github.com/pyxem/orix (GPL-3.0)
diffpy.structure==3.2.0   # crystal symmetry backend for orix
# NOTE: MTEX (MATLAB) is retained ONLY as an optional Phase 4 texture-descriptor
#       exporter run offline. Not part of the runtime pipeline. See docs/mtex_optional.md.

# --- Image processing ---
scikit-image==0.24.0      # morphology, filters, measure (grain properties)
opencv-python-headless==4.10.0.84  # SEM-EBSD registration (ORB, ECC), no GUI deps
Pillow==10.4.0

# --- Deep learning (Phase 2+) ---
torch==2.4.0              # Colab T4-compatible
torchvision==0.19.0
segmentation-models-pytorch==0.3.4  # U-Net / SegFormer / U-Net++ with pretrained encoders
                                     # https://github.com/qubvel-org/segmentation_models.pytorch (MIT)
albumentations==1.4.14    # augmentations (image+mask synchronized)
timm==1.0.9               # encoder zoo used by smp

# --- Classical ML (Phase 4 baseline) ---
scikit-learn==1.5.1
xgboost==2.1.1

# --- Uncertainty (Phase 3/4) ---
# MC Dropout: implemented from scratch in src/uncertainty/mc_dropout.py (no extra deps)
# Deep ensemble: implemented in src/uncertainty/ensemble.py (no extra deps)
# Conformal prediction:
mapie==0.9.1              # calibrated prediction intervals for regression (Phase 4)
# Segmentation conformal: implemented from scratch, reference:
#   https://github.com/NKI-AI/kandinsky-calibration

# --- Config + experiment management ---
PyYAML==6.0.2
hydra-core==1.3.2         # config composition (small-data configs vs scale-up configs)
omegaconf==2.3.0

# --- Logging / visualization ---
matplotlib==3.9.2
seaborn==0.13.2
tqdm==4.66.5
tensorboard==2.17.1       # lightweight, works on Colab

# --- Testing ---
pytest==8.3.3

# --- Feature engineering (Phase 4) ---
# GLCM: scikit-image (already pinned)
# Fourier descriptors: numpy (already pinned)

# --- Web app (Phase 6, optional) ---
# streamlit==1.38.0       # uncomment when reaching Phase 6

# --- Notes on GPU ---
# Colab free provides T4 (16 GB). All model configs are sized to fit under
# batch_size * image_size constraints defined in configs/model/*.yaml.
# If you get CUDA OOM: (1) reduce batch size in config, (2) switch to
# mixed precision (already enabled by default in Phase 2 training script),
# (3) use smaller encoder (efficientnet-b0 → resnet18).
